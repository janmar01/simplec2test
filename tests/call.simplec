

function(pointer<char>, char) -> int printf;

print(ch) : function(char) -> int {
  int garbage;
  garbage = printf("%c", ch);

  return 0;
}

main {
  int garbage;
  char x;
  x = 'a';
  garbage = print(x);
  return 0;
}