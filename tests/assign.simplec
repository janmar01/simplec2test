function(pointer<char>, char) -> int printf;


print(ch) : function(char) -> int {
  int garbage;
  garbage = printf("%c", ch);
  return 0;
}

main {
  char x;
  int res;
  x = 'c';
  res = print(x);
  return 0;
}
